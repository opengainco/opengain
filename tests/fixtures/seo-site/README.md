# Acme Forms fixture

This is a static test fixture, not a production website. It intentionally contains SEO defects. Hosts should inspect the files without network access.

The expected evidence is documented in `EXPECTED.md` so acceptance results are reproducible.
